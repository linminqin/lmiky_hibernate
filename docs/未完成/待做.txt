初始化解析模块配置文件时，保存的顺序不是按配置文件的顺序
地区管理，检查是否有子节点，用了isEmpty，如果数据多了，可能会影响速度，关于图片的切换也可能会影响速度
上传文件放在临时目录，保存的时候才转移，定时清除无用临时文件
手工刷新页面还会重新执行原先的提交表单
内容页面iframe会根据内容自动拉高，但不会自动缩短
设置权限的时候，如果父模块已经设置了某个角色，则子模块应该也显示该角色已被选中